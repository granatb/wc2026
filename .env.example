# Copy to .env and fill in. Never commit real keys.
# The Odds API — https://the-odds-api.com  (match odds + player props)
ODDS_API_KEY=

# API-Sports / api-football — https://www.api-football.com  (schedule + lineups)
API_FOOTBALL_KEY=
