# no trailing slash: also ignores the data->main-checkout symlink used in worktrees
data
dist/
__pycache__/
*.pyc
.env
.DS_Store
.wrangler/
# the MCP server's installed dependency tree (package-lock.json IS committed)
node_modules/
