# The evmax FPL dataset

Every Fantasy Premier League point projection evmax publishes, as bulk JSON and
CSV. One file per gameweek plus a cumulative file, free under **CC BY 4.0**.

Human page: <https://evmax.ai/data/>

> This file mirrors the schema. The single source of truth is
> `evmax/dataset.py` — `CSV_COLUMNS` for the order and `COLUMN_GLOSSARY` for
> the definitions. `/data/` renders from that same dict, and
> `tests/test_dataset.py` fails if a column ever ships without a definition.
> When a column is added, update this table in the same commit.

## Files

| URL | What it is |
|---|---|
| `/api/fpl/dataset/index.json` | The manifest: which gameweeks exist, both file paths for each, and the full column schema. **Read this first.** |
| `/api/fpl/dataset/gw{N}.json` | One gameweek — meta block plus every simulated player. |
| `/api/fpl/dataset/gw{N}.csv` | The same gameweek, flat. |
| `/api/fpl/dataset/all.json` | Every gameweek published so far, in one file. |
| `/api/fpl/dataset/all.csv` | The same, flat. |

Base URL: `https://evmax.ai`.

## Update cadence

Regenerated by every site build, which runs before each Premier League deadline
— so roughly weekly, and again whenever the model or the squad changes before
lock.

**Past gameweeks are never rewritten.** The cumulative file is rebuilt from the
per-gameweek files already on disk, not by re-simulating. A projection published
before a deadline stays exactly as it was published — that is what makes the
[accuracy page](https://evmax.ai/fpl/accuracy/) meaningful.

## JSON shape

```jsonc
{
  "gameweek": 2,
  "count": 610,
  "meta": {
    "license": "CC BY 4.0",
    "license_url": "https://creativecommons.org/licenses/by/4.0/",
    "attribution": "evmax (https://evmax.ai)",
    "attribution_line": "Data: evmax (https://evmax.ai), CC BY 4.0 — https://creativecommons.org/licenses/by/4.0/",
    "source": "https://evmax.ai/data/",
    "method": "Market odds (de-vigged) to Dixon-Coles scorelines to 50,000 Monte-Carlo simulations …",
    "generated_at": "2026-08-26T09:17:11+00:00",
    "has_distributions": false
  },
  "players": [
    { "gameweek": 2, "id": 426, "name": "B.Fernandes", "team": "MUN", "…": "…" }
  ]
}
```

`all.json` has the same shape with `"gameweeks": [1, 2, …]` in place of
`"gameweek"`. Players are ordered by gameweek ascending, then projected points
descending, then name — so the file is byte-stable across rebuilds.

## Columns

The CSV header is stable. Columns are only ever **added at the end**, never
reordered or removed, so a script reading by position keeps working. An empty
cell means we could not compute that number for that player — **an empty cell is
never a zero**.

| Column | Type | Meaning |
|---|---|---|
| `gameweek` | integer | Premier League gameweek the projection is for. |
| `id` | integer | The official FPL element id — the stable join key against the FPL API. Empty if we could not match the player to the bootstrap feed. |
| `name` | string | Player name as our model carries it, disambiguated when two players share a surname. |
| `team` | string | Three-letter club code (ARS, MUN, …). |
| `position` | string | GK, DEF, MID or FWD. |
| `price` | float | FPL price in millions at the time of the build. |
| `ownership_pct` | float | Percent of FPL managers owning the player at build time. |
| `x_points` | float | Our projection: mean FPL points across 50,000 simulations of this gameweek. The headline number. |
| `captain_ev` | float | Expected points if captained (2 × `x_points`). |
| `ceiling` | float | Tail mean — the average of the player's best 15% of simulations. The realistic good week, not a cap. |
| `value` | float | `x_points` per million of price. |
| `bonus` | float | Expected bonus points, per match. |
| `defcon` | float | Expected defensive-contribution points, per match. |
| `p_defcon` | float | Probability of hitting the defensive-contribution threshold, per match (`defcon` = 2 × `p_defcon`). |
| `cs_points` | float | Expected clean-sheet points, per match. |
| `start_prob` | float | Probability the player starts, from the priors (research notes can override it). |
| `kickoff` | string | ISO-8601 UTC kickoff of the player's fixture. |
| `verdict_tier` | string | S / A / B / C / D — the player's band this gameweek by projected points. |
| `verdict_call` | string | buy / hold / pass — the tier read as a call. |
| `p10` | float | 10th-percentile outcome (the floor). Empty for gameweeks published before we stored distributions. |
| `median` | float | Median simulated points. Empty for gameweeks published before we stored distributions. |
| `mode` | float | Most likely single point total. Empty for gameweeks published before we stored distributions. |
| `p90` | float | 90th-percentile outcome. Empty for gameweeks published before we stored distributions. |
| `p_haul` | float | Probability of 10 or more points. Empty for gameweeks published before we stored distributions. |
| `p_blank` | float | Probability of 2 or fewer points. Empty for gameweeks published before we stored distributions. |

### JSON-only field

| Field | Type | Meaning |
|---|---|---|
| `distribution` | object | The sparse point-mass function over integer FPL points, `{"points": count}` across the 50,000 simulations. Omitted for gameweeks published before we stored it, and never a CSV column — a nested object has no honest flat representation. |

### A units warning

`x_points` (and therefore `captain_ev`, `value` and `ceiling`) is a **per-week**
total: a double-gameweek player's figure covers both fixtures. `bonus`,
`defcon`, `p_defcon` and `cs_points` are **per-match** averages, so for a doubled
player they do not sum to `x_points`. Do not present them as components of the
weekly total without checking the player's fixture count.

## CSV format

RFC 4180: header row, `CRLF` line endings, minimal quoting (a value containing a
comma, quote or newline is quoted and internal quotes doubled), UTF-8, no index
column.

## Licence

**CC BY 4.0** — <https://creativecommons.org/licenses/by/4.0/>

Use it for anything, commercial work included. The one condition is attribution.
Paste this:

```
Data: evmax (https://evmax.ai), CC BY 4.0 — https://creativecommons.org/licenses/by/4.0/
```

For a paper or a README:

```
evmax. "FPL projections dataset." https://evmax.ai/data/ Licensed CC BY 4.0.
```

## How the numbers are made

De-vigged market odds → Dixon-Coles scorelines → 50,000 Monte-Carlo simulations
per gameweek → scored on the official Fantasy Premier League points table.
Projections are frozen before the deadline and graded publicly afterwards:
<https://evmax.ai/fpl/accuracy/>.

## Access from an agent

There is an MCP server over this data in [`mcp/`](../mcp/README.md):

```bash
claude mcp add evmax -- npx -y evmax-mcp
```

## Not affiliated with the Premier League or Fantasy Premier League.
