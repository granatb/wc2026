**Implementation scope — 7 September 2026**

The user authorized the full remediation and strategy work. Implementation will preserve historical forecasts and identify corrections rather than relabel new simulations as old predictions. No paid subscription is required for the first implementation. Model improvement is an experiment; winning performance cannot be declared before independent results exist.

| Priority | Deliverable | Acceptance |
|---|---|---|
| P0 | Forecast archive and historical squad replay | Post-deadline builds never simulate historical forecasts; full future boards, ordered squads, identities and provenance are archived; legacy gaps are labelled |
| P0 | Grading and benchmark integrity | Finality required; stable IDs where available; common populations; paired metrics; late evidence rejected; amendments preserved |
| P0 | Scoring defects | MID clean sheets, scorer/assister consistency, BPS residuals, exposure/minutes, DC scorelines, DefCon shrinkage and saves covered by scenario tests |
| P1 | Cache/competition isolation | FPL fixtures isolated; position/team included in fingerprints; current metadata refreshed independently |
| P1 | Transparent public record | Round 3 restored; unsupported accuracy/noise claims removed; GW3 late Git receipt disclosed; forecast versions distinguished |
| P1 | Security and repeatable deployment | Vulnerable dependency updated; deployment pinned; response timeout and safe links; security headers; archive completeness checked |
| P1 | Data and experimentation foundation | Timestamped observations, structured evidence, chronological evaluation, naive/statistical challenger and odds/research comparison support |
| P1 | Decision utility | Transfers valued through legal weekly XIs and captaincy; uncertainty/assumptions explicit |
| P2 | Product focus | Clear methodology, evidence-led decision explanation and review workflow, measurable retention plan |
| P2 | Further research-dependent work | Historical ingestion/coverage audit, trained minutes model, calibrated model combination, paid-data trials only where justified, prospective evaluation |

Execution order: protect the record; correct and test the mathematics; improve utility and evidence capture; integrate build/security/public explanations; run the full suite and a clean preview. Record implemented scope and outstanding empirical work here at completion. Do not claim a trained or validated improvement from a scaffold or a backtest with unavailable historical inputs.


## Delivery status

Implemented: P0 archive/replay and grading protections; scoring corrections with
new scenario tests; P1 cache isolation, transparent record, MCP dependency patch,
response-body timeout, safe links, security headers, pinned deploy CLI and a
publication-evidence gate; legal-XI/captain transfer utility; timestamped official
input observations; a functioning chronological odds-free ridge evaluation.

New production forecasts now require hashed bootstrap/fixture observations and
market captures within 24 hours. This is an operational freshness policy, not a
statistical claim. Historical replays use their archive. Full new archives retain
input data and model settings, plus player cards and prose. Legacy exports without
pre-deadline evidence are explicitly unavailable. No historical claims were
re-simulated. The original grading receipts remain alongside amendments.

Research-dependent scope remains open: fit/validate the minutes and joint-lineup
models; integrate and compare the challenger with the corrected production model
at matching cutoffs; tune blends on prior folds; model autosubs/vice/chips; acquire
and audit deeper event data; demonstrate prospective improvement and product
retention. These need new data and future outcomes. They are not represented as
completed fixes. Optional Streamlit/Anthropic dependency pinning and a dedicated
frontend dependency audit remain outside the tested stdlib static-site/MCP path.

The detailed product/data roadmap and measured results are in
`2026-09-07-strategy-and-results.md`. The new model remains a candidate. Its GW4
smoke artifact used dated cached odds; it was not published as a current forecast.

## Verification completed

- Full Python suite after experiment follow-up: **1,305 tests passed** (28.970 seconds).
- MCP regressions: **3 passed**, covering slow response bodies, HTML fallback and unavailable-dataset handling.
- MCP lockfile audit / clean installation: **0 reported vulnerabilities**.
- Historical GW3 preview built successfully at `/private/tmp/wc2026-review-site`; `scripts/validate_site.py` passed.
- Fresh-model smoke run: 5,000 simulations, 651 players, 10 GW4 fixtures. Cached odds dated 1 September; test output only.
- All three historical squads replay to their original official totals; three original grading receipts preserved.
- Shell/JavaScript syntax checks and `git diff --check` passed.

The engine determinism digest changed intentionally and is documented in
`CHANGELOG.md`. One probability-summary assertion now allows the mathematically
valid 0.01 difference between independently rounded components and their rounded
total. The full site generation tests explicitly exercise a pre-deadline clock;
separate integration tests forbid any simulation during a locked historical build.

Changes are local and uncommitted. No production deployment or paid data purchase
was performed. The reviewed legacy preview intentionally lacks fabricated player
cards and full-board exports; existing article forecasts remain available.

## Follow-up: merge integrity and experiment foundation

Implemented `games/fpl/experiments.py`, its prepare/freeze/grade/report CLI, a
registered four-arm protocol and the public `/fpl/experiments/` summary. Virtual
portfolios carry bank, purchase/selling prices, transfers and hits between weeks.
The same one-transfer policy selects lineups and captains; deviations require
dated evidence. Full common-player forecasts, matching input hashes, pre-deadline
capture, consecutive history and final outcomes are enforced. No weeks are enrolled
and no providers are represented as active. See `experiments/fpl-2026-27/README.md`
for the input contract and activation procedure.

Also fixed the deployment validator's missing-captains discovery gap and archive
embedded-ID verification; atomic writes now use unique temporary files.
The next distinct scope is provider integration and calibration: corrected market,
odds-free statistics, a previously tuned blend and a defined consensus forecast.
Merge/review this foundation before expanding that modeling change. Registration
alone does not satisfy prospective validation or identify next season's winner.
